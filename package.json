{
  "name": "classroom-copier-root",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "build": "npm run build --workspace=shared && npm run build --workspace=client"
  }
}
